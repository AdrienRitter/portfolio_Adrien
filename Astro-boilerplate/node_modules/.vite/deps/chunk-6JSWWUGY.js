// node_modules/astro/dist/runtime/client/events.js
var HYDRATE_KEY = `astro:hydrate`;
function debounce(cb, wait = 20) {
  let h = 0;
  let callable = (...args) => {
    clearTimeout(h);
    h = setTimeout(() => cb(...args), wait);
  };
  return callable;
}
var notify = debounce(() => {
  if (document.querySelector("astro-root[ssr]")) {
    window.dispatchEvent(new CustomEvent(HYDRATE_KEY));
  }
});
var listen = (cb) => window.addEventListener(HYDRATE_KEY, cb, { once: true });
if (!window[HYDRATE_KEY]) {
  if ("MutationObserver" in window) {
    new MutationObserver(notify).observe(document.body, { subtree: true, childList: true });
  }
  window[HYDRATE_KEY] = true;
}

export {
  notify,
  listen
};
//# sourceMappingURL=chunk-6JSWWUGY.js.map
