import type { ReactNode } from 'react';
declare type IProjectProps = {
    img: {
        src: string;
        alt: string;
    };
    name: string;
    description: string;
    link: string;
    category: ReactNode;
};
declare const Project: (props: IProjectProps) => JSX.Element;
export { Project };
