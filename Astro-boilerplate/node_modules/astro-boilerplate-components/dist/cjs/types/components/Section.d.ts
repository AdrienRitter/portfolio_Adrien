import type { ReactNode } from 'react';
declare type ISectionProps = {
    title?: ReactNode;
    children: ReactNode;
};
declare const Section: (props: ISectionProps) => JSX.Element;
export { Section };
