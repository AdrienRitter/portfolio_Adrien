import type { ReactNode } from 'react';
declare type INewsletterProps = {
    title: ReactNode;
    description: ReactNode;
};
declare const Newsletter: (props: INewsletterProps) => JSX.Element;
export { Newsletter };
